{% extends 'base.html.twig' %}

{% block title %}Ajouter un terrain{% endblock %}

{% block body %}

    {{ path('court_new') ? ('<body class="background-court-img">') : '' }}

    {{ form_start(form,{ 'attr': {'class': 'form-login'}  }) }}
    <h1>Ajoutes un terrain</h1>
    {{ form_widget(form) }}
    <button class="btn btn-login" type="submit">
        Ajouter le terrain
    </button>
    {{ form_end(form) }}
{% endblock %}
